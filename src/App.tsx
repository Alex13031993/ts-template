import React from 'react';
import { Button } from 'styles/global.styles';

const App: React.FC = () => (
  <>
    <div>
      <Button background="blue" color="red">
        Кнопка
      </Button>
    </div>
  </>
);

export default App;
